syntax = "proto3";

package game;

message Game {
    string id = 1;
    repeated Player players = 2;
}

message Player {
    string id = 1;
    string name = 2;
    float size = 3;
    Position position = 4;
}

message Position {
    string x = 1;
    float y = 2;
}
